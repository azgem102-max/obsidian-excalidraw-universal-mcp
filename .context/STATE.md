# حالة الحزمة

> آخر تحديث: 2026-08-05 — Codex

- الحزمة توفر خادم MCP محليًا موحدًا لـObsidian وExcalidraw باسم `excalidraw`.
- نسخة التطوير الحالية للجسر والخادم `0.6.0`، وعدد الأدوات 59.
- البيئة الحية: Obsidian 1.13.4، Excalidraw 2.25.3، Extras 0.0.15.
- المحتوى المثبت: 17 سكربت تخطيط أساسيًا و15 سكربتًا احترافيًا.
- `install.mjs` يثبت الإضافات والجسر والمحتوى ويضبط Claude/Codex ومشروع MCP.
- `doctor.mjs` يثبت جاهزية النسخ والإضافات واتصال الجسر الحي.
- نجحت فحوص المصدر والحزمة للإصدار `0.6.0`: ‏56/56 على Windows، والاختبار الذاتي يثبت عرض 59 أداة.
- نجحت جولتا القبول الحي الأساسيتان 53/53 لكل جولة بعد إعادة تشغيل Obsidian.
- نجح اختبار الأدوات المكمل 11/11، بما فيه المسح والاستعادة والمشاركة الخارجية لرسم اصطناعي.
- نجحت 4 سكربتات احترافية قابلة للأتمتة؛ 9 تفاعلية و2 يعتمدان على مدخلات/أصول خارجية موثقة بوضوح ولا تُحسب ناجحة آليًا.
- أثبت التصدير SVG تضمين الخط المحلي الرابع بصيغة WOFF2 وظهور العربية، وراجعت عينات PNG بصريًا.
- نتائج الاختبار الكاملة في `ACCEPTANCE-TEST-MATRIX.md`، وخطة النشر في `.context/plans/2026-08-03-public-repository.md`.
- اجتازت نسخة النشر المستقلة فحص `npm run check`: ‏25/25 اختبارًا و59 أداة، ولم يُعثر على رموز وصول أو خطوط خاصة أو بيانات Vault.
- المستودع العام: `https://github.com/azgem102-max/obsidian-excalidraw-universal-mcp`.
- أُعيدت كتابة واجهة `README.md` بلغة عربية بسيطة وعنوان مباشر، مع قسم واضح ينسب النتيجة إلى Obsidian وExcalidraw for Obsidian وExtras ومكتبة السكربتات وExcalidraw core ومرجع MCP، بروابط مباشرة لكل مشروع.
- أصبح المثبّت يميز WSL عن Linux العادي: يكتب إعداد Claude Desktop باستخدام `node.exe` ومسارات Windows، ويرفض المسارات غير المشتركة، ويوفر `install-on-windows.ps1` للجلسات المنفصلة؛ تغطي ذلك 5 اختبارات جديدة.
- أضيف `setup-windows.cmd` للتثبيت بنقرتين: يختار المستخدم الـVault من نافذة، ويتجاوز عائق PowerShell، ويطلب الموافقة قبل تثبيت Node.js عند غيابه.
- راجعت حزمة تسليم Claude للإصدار `0.6.0` وطُبقت التحسينات مع إصلاح معرّفات العناصر والربط بين الأسهم والأشكال، وتمرير مسار Node، واكتشاف إعداد Claude في نسخ Windows المختلفة.
- شغّل Claude الإصدار `0.6.0` داخل Vault حقيقية بنسخة MSIX وتحقق حيًا من الخط العربي، حماية المسارات، ربط الأسهم، الحفظ وإعادة الفتح، ونجح `validate-scene --strict`.
- راجع Codex فرع `claude/agent-portability-fixes` واكتشف وأصلح قابلية استيراد اختبار Windows، وإعادة إدخال عميل مستبعد، وعدم تطابق فحص امتداد الخط بين الجسر والطبيب.
- نجح تثبيت معزول فعلي على Windows للمسارين التقليدي وMSIX؛ كِلاهما سجل Node والخادم والخزنة الصحيحة، وMSIX لم يكتب في مسار Claude التقليدي.
- نجح `create_from_mermaid` حيًا مرة واحدة عند 36 عقدة/123 عنصرًا، و220 عقدة/745 عنصرًا، و500 عقدة/1499 عنصرًا. الحالة الأخيرة استغرقت 38 ثانية وبقي العدد ثابتًا بعد الحفظ والفحص، بلا تكرار.
- أُصلح عقد Mermaid: الطابور ينتظر العملية الحقيقية، وإعادة الطلب المطابق عشر دقائق تعيد النتيجة بدل المضاعفة، والتكرار المقصود يحتاج `forceDuplicate: true`.
- صار `validate-scene --strict` يقبل nanoid الرسمي بطول 21 لغير النص، مع إبقاء النص عند 8 محارف لحماية `Text Elements`.
- نجح الاختبار الحي النهائي في رسم واحد: الطلب الأول أنشأ 17 عنصرًا والثاني المطابق أعادها بلا زيادة، ثم حُسّن الرسم إلى 19 عنصرًا وحُفظ ونجح الفحص الصارم بلا ملاحظات.
- اجتازت الحزمة بوابة ما قبل النشر: 56/56 اختبارًا و59 أداة، وأثبت الطبيب الجاهزية الكاملة، ثم حُفظ الرسم النهائي وانتُقل عنه وأُعيد فتحه وبقيت عناصره الـ19 سليمة بصريًا ومن القرص.
- اجتاز Claude Desktop اختبار المستخدم الجديد: أنشأ ملاحظة ورسمًا عربيًا من 9 عناصر، وحفظه وانتقل عنه وأعاده بلا فقد أو مضاعفة مع بقاء ربط السهمين.
- أُغلقت ملاحظتا القبول: يفتح `create_drawing` الرسم افتراضيًا، و`read_note.tags` يجمع وسوم المتن وFrontmatter مع حقلي المصدر؛ تحقق السلوكان حيًا وصارت البوابة 58/58.
- نُقلت مجلدات الاختبارات السابقة الخمسة (81 ملفًا) إلى `.trash/اختبارات سابقة 2026-08-05 115826`، وهي قابلة للاستعادة، وبقي مجلد `الاختبار النهائي` وفيه رسم واحد فقط.

## التالي

- مرشّح `0.6.0` جاهز لاعتماد المستخدم: يُدمج أولًا في `codex/development` ثم في `main` بلا كتابة قسرية للسجل.
- متابعة تطوير اختبارات الواجهة للسكربتات الاحترافية التفاعلية من دون قرارات أو بيانات شخصية.
