# سجل التغييرات

## 0.6.0 — تكملة: الخط، والمهلة، ومسارات العملاء الثلاثة

### قبول Claude Desktop الأخير — 2026-08-05

- صار `create_drawing` يفتح الرسم الجديد افتراضيًا، فلا تذهب أدوات المشهد إلى
  رسم سابق إذا نسي الوكيل تمرير `open: true`. ويبقى الإنشاء الصامت متاحًا بخيار
  `open: false` مع `nextAction` صريح.
- صار `read_note.tags` يجمع وسوم المتن وFrontmatter، مع حقلين منفصلين
  `inlineTags` و`frontmatterTags` لمن يحتاج معرفة المصدر.
- أضيف دليل مستقل وقصير لـCodex/ChatGPT المحلي، ودليل آخر يفصل Claude Desktop
  عن Claude Code؛ ويربط بهما README وصفحة البداية، وتذكّر أداة Windows بخطوات
  إعادة التشغيل وموصل Claude Desktop بعد نجاح التثبيت.

### مراجعة Codex على Windows — 2026-08-05

بعد استيراد commits الثمانية وتشغيل الحزمة من نسخة نظيفة على Windows ظهر فشل
واحد في بوابة الاختبار نفسها، ثم كشفت مراجعة مسار التنفيذ فجوتين إضافيتين:

- صار الاستيراد الديناميكي في الاختبار يستخدم رابط `file://` صالحًا على Windows.
- تصل قائمة العملاء بعد الاستبعاد فعلًا إلى `configureClients` بدل إعادة بناء
  القائمة الأصلية وإعادة إدخال Claude Desktop المتعذّر.
- صار `doctor` يشترط امتداد ملف خط مثل الجسر، فلا يُعامل الاسم الافتراضي
  `Virgil` كمسار خط مخصص.
- طُبقت صيغة `RESULT` الفعلية، بما فيها `client=registered`، في وثائق الدخول.

النتيجة بعد الإصلاح: **54/54**، و**59 أداة**، و`validate-scene --strict` سليم.

### بوابة Mermaid وWindows النهائية — 2026-08-05

- نجح التثبيت المعزول على Windows للمسارين التقليدي وMSIX، واختار كل مسار ملف
  Claude الصحيح مع Node والخادم والخزنة الفعلية.
- نجح تحويل 500 عقدة و499 سهمًا إلى 1499 عنصرًا خلال 38 ثانية، وبقي العدد ثابتًا
  بعد الحفظ والفحص. أثبت ذلك أن مؤقت JavaScript لا يستطيع مقاطعة عمل متزامن يحجب
  خيط واجهة Obsidian.
- صار انتهاء مهلة الرد لا يحرر طابور الجسر؛ يبقى مربوطًا بالعمل الحقيقي حتى ينتهي.
- صار طلب Mermaid المطابق في الرسم نفسه يُعاد استخدام نتيجته عشر دقائق بدل إنشاء
  نسخة ثانية. التكرار المقصود متاح فقط بـ`forceDuplicate: true`.
- صار المدقق يقبل nanoid الرسمي بطول 21 للأشكال والأسهم التي يولدها Mermaid، مع
  إبقاء معرّفات النصوص صارمة عند 8 محارف لحماية قسم `Text Elements`.

مراجعة ثانية لمرشّح 0.6.0. **لا ترفع رقم الإصدار بقصد**: المرشّح لم يُصدَر بعد،
فهذه البنود جزء منه لا إصدار تالٍ له. و**البند الأول انحدار أدخله إصلاح 0.6.0
نفسه**، والبقية فجوات أظهرها.

### 🔴 استعادة الأسلوب كانت تُلغي خط المستخدم

استعادة الأسلوب القياسية في 0.6.0 أضافت `fontFamily: 1`. فكل عنصر لا يمرّر
`fontFamily` صريحًا **يفقد الخط العربي** ويخرج بحروف مفكّكة — رغم أن المستخدم
ثبّته بـ`--font` وExcalidraw يحمله افتراضًا للخزنة.

والمسألة أدق من حذف المفتاح، لأن **ثلاث حالات تتنافس** ولكل خيار ثابت خسارة:

| الخيار | ما يكسره |
|---|---|
| فرض `1` (سلوك 0.6.0) | يُلغي خط من ثبّته — الانحدار نفسه |
| فرض `4` | يضيّع النص على خزنة بلا خط؛ العائلة غير موجودة |
| عدم الاستعادة إطلاقًا | عنصر واحد يمرّر `fontFamily: 3` يجعل **كل ما بعده** بالخط نفسه — وهو تسرّب الحالة الذي وُضعت الاستعادة لمنعه |

فالحل ليس رقمًا: **يُحسب الهدف من أوثق مصدر متاح، ويعود إليه كل عنصر** — وتفصيله
في القسم التالي، لأن أول محاولة له كانت خاطئة أيضًا. فينجو خط المستخدم أيًّا كان،
ولا يتسرّب خط عنصر إلى ما بعده، ويبقى التمرير الصريح عاملًا.

و`fontSize` **يبقى على الاستعادة العادية** (`20`): مقاس عنصر ليس تفضيلًا يملكه
المستخدم، وعنوان بـ72 بكسل يجب ألا يسري على ما بعده. إخراجه مع العائلة كان سيكون
تسرّبًا ثانيًا بلا مقابل.

### ما كشفه فحص مصدر Excalidraw 2.25.3 نفسه

قبل أي تشغيل حيّ، قُرئ مصدر إضافة Excalidraw المثبَّتة (4.9MB) للتحقّق من
الافتراضات لا الاكتفاء بها. فظهر عطلان في الإصلاح أعلاه:

**١. القيمة الافتراضية للخط الرابع هي الاسم المجرّد `"Virgil"` لا مسار ملف.**
`experimantalFourthFont:"Virgil"` في تعريف الإعدادات الافتراضية. فأي خزنة لم يُضف
إليها خط تُقرأ «الخط الرابع مسجَّل وملفه مفقود» — **إنذار كاذب يصيب أكثر
المستخدمين**، وهو نفس صنف «النجاح الكاذب» الذي جاء هذا العمل ليزيله، مقلوبًا.
فصار الشرط لاحقةَ ملف خط (`otf`/`ttf`/`woff`/`woff2`) — وهي التي يقبلها
`install.mjs` — فيُفرَّق «غير مثبَّت» عن «مسجَّل وملفه مفقود» فرقًا حقيقيًا.

**٢. `ea.style` تُنشأ بـ`fontFamily: 1` مثبَّتًا في الكود**، لا مشتقًّا من إعداد
الخزنة. فالاعتماد على «أول قيمة نراها» كان قد يثبّت `1` على خزنة خطها العربي
مفعَّل — أي الانحدار نفسه بطريق أطول. فصار الهدف يُحسب من أوثق مصدر متاح، بثلاث
طبقات كل واحدة قد تفشل فتُسلّم للتالية:

1. `currentItemFontFamily` من واجهة العرض — ما كان المستخدم سيحصل عليه لو كتب بيده.
2. إعداد الخزنة: خط رابع مفعَّل وملفه موجود ⇒ `4`.
3. أول قيمة رُئيت قبل أي تعديل.

و`null` تعني: لا تلمس المفتاح. لا يخمّن الجسر رقمًا في أي حالة.

### ما كشفه التشغيل الحيّ — وهو ما لم يكشفه شيء قبله

ثُبِّت 0.6.0 في خزنة حقيقية وشُغِّل. فتأكّد أوثق ما في الفرع، وسقط افتراض واحد:

**تأكّد:** `status.fonts` يعمل مقابل الإضافة الحقيقية (`arabicFontFamily: 4`،
`fileFound: true`، المسار والاسم صحيحان) — وهو الافتراض الأخطر الذي لم يكن مُتحقَّقًا.
ولا تسرّب مقاس ولا خط بين العناصر. وإعادة توجيه ارتباط العنوان تعمل: مُرِّر معرّفا
**العنوانين** فرجع الارتباط على **الحاويتين**. و`safePath` يرفض
`_to_delete/../../../Desktop/pwned.md`. و`ALIAS_SHADOWS_ELEMENT` يرفض اسمًا يطابق
معرّفًا قائمًا. والحفظ ثم التنقّل ثم العودة أبقى 11 عنصرًا كما هي — وهو الفحص الذي
أتلف ملفًا قبل هذه الإصلاحات — و`validate-scene --strict` عليه: **سليم تمامًا**.

**وسقط:** `currentItemFontFamily` في الخزنة الحقيقية كان `1` لأن صاحبها يطبّق خطه
العربي بسكربت **بعد** الكتابة لا باختياره من الشريط. فطبقة «اختيار الواجهة» — وهي
الأوثق منطقيًا — كانت تُخرج **كل نص عربي بالعائلة 1**، والعائلات 1-3 لا تحمل محارف
عربية إطلاقًا فيخرج مفكّكًا. أي أن الترتيب كان صحيحًا نظريًا وخاطئًا عمليًا.

فأُضيفت قبل الطبقات كلها حالةٌ **ليست تخميناً بل قراءةَ محتوى**: إن كان النص عربيًا
وفي الخزنة عائلةٌ تحمل العربية، فهي العائلة **الوحيدة** التي تعرضه — واختيارها ليس
ترجيحًا بين تفضيلين بل الفرق بين نصٍّ مقروء وغيره. ويبقى: اللاتيني لا تُفرض عليه،
والتمرير الصريح يفوز عليها، وخزنة بلا خط عربي لا يُفرض عليها `4`. الأربعة مقيسة حيًّا
ومحروسة باختبار.

### 🟠 لا أداة كانت تُبلّغ عن الخط — فكان الوكيل يخمّن

`AGENTS.md` كان يقول «استخدم `fontFamily: 4` عندما يكون الخط المحلي مفعّلًا» —
**وهو شرط لا يستطيع الوكيل التحقّق منه**: لا `status` يذكره ولا `doctor` يفحصه.
فيخمّن، وأي تخمين يخسر إحدى الحالتين: فرض `4` على خزنة بلا خط يضيّع النص، وتركه
يورث ما تفرضه الحالة العامة.

- **`status` يعيد `fonts`** — العائلات الأربع، و`arabicFontFamily` (`4` أو `null`)،
  و`guidance` نصية. ويميّز «غير مثبَّت» من «مسجَّل وملفه مفقود» فلا يُبلّغ بجاهزية
  كاذبة.
- **`doctor.mjs` يطبع سطر الخط** بالعربية والإنجليزية، يقرأه من إعدادات Excalidraw
  على القرص فيعمل مع `--skip-live` وبلا Obsidian مفتوح. **معلومة لا شرط:** الخط
  اختياري فلا يؤثر في `installReady` ولا في رمز الخروج.
- `AGENTS.md`: «اقرأ الخط من `status`، لا تفترض رقمًا».

### 🟠 قاعدة المهلة كانت في ملف لا يقرأه الوكيل

المهلة **لا توقف العملية** داخل Obsidian: تُرجع خطأً والعمل يكمل ثم يكتب في
المشهد. وكانت الإعادة قد تضاعف ما أُنشئ — و`create_from_mermaid` أكثر ما يقع فيه. كانت
القاعدة موثّقة في `docs/TROUBLESHOOTING-AR.md` وحده، وهو **ملف يقرأه المستخدم لا
الوكيل**، و`AGENTS.md` لا يذكر المهلة إطلاقًا.

نُقلت إلى `AGENTS.md`، ثم أثبت القياس الحي أن مؤقت 30 ثانية لا يتقدم عندما تحجب
Mermaid خيط الواجهة. صار الطابور ينتظر العمل الحقيقي، وصارت إعادة Mermaid المطابقة
تعيد النتيجة السابقة بدل مضاعفة المشهد، مع إبقاء `describe_scene` خطوة تحقق واضحة.

### 🟠 مسار Codex ليس مسار Claude Desktop

العملاء الثلاثة مدعومون بالتساوي، **لكن خطوات ما بعد التثبيت تختلف** — ومفتاح
تفعيل الموصل **إلزامي في Claude Desktop وحده**. كانت الوثيقة تعرض مسارًا واحدًا
بملاحظات، فيتبع مستخدم Codex خطوة لا تخصّه أو يفوّت مستخدم Desktop خطوة تخصّه.

- `START-HERE-AR.md`: جدول **«اختر عميلك أولًا»** بثلاثة أعمدة — اسم العميل في
  المثبّت، وملف الإعداد، ونطاقه، وملف التعليمات، ومفتاح الموصل، وسلوك إعادة
  التشغيل.
- خطوة الموصل صار عنوانها «**Claude Desktop فقط**» ونصّها يُخرج العميلين الآخرين
  صراحةً.
- `docs/TROUBLESHOOTING-AR.md`: «العميل لا يرى الأدوات» صار ثلاثة مسارات منفصلة.

### 🟡 غياب أداة الترميم صار موثّقًا بدل أن يكون صمتًا

إصلاحات 0.6.0 تمنع **الإصابة الجديدة** ولا تشفي ما أُنشئ قبلها. **وغياب أداة
الترميم متعمَّد ومُعلَن الآن:** كُتبت واحدة بعناية فأفرغت نصوص 13 ملفًا حقيقيًا،
لأنها حدّثت JSON المضغوط دون قسم `## Text Elements`. وسُحبت.

فوُثِّق المسار الآمن بدلًا منها: `validate-scene.mjs` للفحص (قراءة فقط)، ثم إعادة
إنشاء الأسهم عبر أدوات MCP للملفات المهمة، وترك غيرها — مع التحذير من تعديل ملف
مفتوح في Obsidian لأنه يحفظ دوريًا فيكتب فوق التعديل بل يدمج معه فيضاعف العناصر.

### ما كشفه فحص خصومي بالتنفيذ — لا بالنظر

سُلّم الفرع إلى مراجعة مستقلة مهمّتها **إسقاطه** لا إقراره، تُحمّل `main.js` كاملًا
داخل غلاف CommonJS مع `require("obsidian")` مُقلَّدًا، وتحكم على المشاهد الناتجة
بـ`checkScene` المستخرجة من `validate-scene.mjs` نفسه — أي بقواعد المستودع لا برأي
المراجع. فظهرت خمسة أعطال، كلها مبرهنة بالتنفيذ:

- **🔴 `safePath` كان يسمح بالخروج من الخزنة.** يرفض `..` في **البداية** وحدها،
  فيمرّ `Notes/../../../Desktop/pwned.md` إلى `vault.create`، ويمشي `export_image`
  بـ`createFolder` خارج الخزنة. و`normalizePath` في Obsidian يوحّد الشرطات **ولا
  يحلّ `..`**، فلا شيء يوقفه بعد ذلك. والمفارقة أن الفحص الصحيح موجود في الملف
  نفسه على بعد أسطر. صار `..` مرفوضًا في أي موضع، ومعه المسار المطلق وحرف القرص:
  `normalizePath` يقصّ الشرطة الأولى فيصير `/etc/passwd` مسارًا داخل الخزنة، ولا
  هروب فيه لكن إعادة التفسير الصامتة مفاجأة من الصنف الذي نزيله.
- **🔴 مسارا الإنشاء يربطان الأسهم بالعناوين لا بالحاويات.** `updateElement` يُعيد
  التوجيه من نص داخل حاوية إلى الحاوية — بتعليق صريح — و`create_element`
  و`batch_create_elements` لا يفعلان. فالمدخل نفسه يُنتج نتيجتين، وناتج الإنشاء
  يرصده `validate-scene` خطأً باسم `binding-to-label`. ويقع كثيرًا لأن الوكيل يأخذ
  المعرّف من `describe_scene` فيقع على معرّف النص. أُخرج المنطق إلى
  `redirectLabelBindings` ويستخدمه المسارَان.
- **🟠 الربط بعنصر محذوف كان مقبولًا.** `copyBindingTargetsToWorkbench` لا يرشّح
  `isDeleted`، فيرتبط السهم بشاهدة ويصير `startBinding` معلّقًا — و`updateElement`
  يرفض المدخل نفسه. صار الترشيح في الجهتين.
- **🟠 اسم مستعار مجهول في `frameId` كان يُكتب خامًا.** الأسهم تفشل صريحًا على اسم
  غير محلول، أما `frameId` فيصير مرجع إطار معلّقًا **لا يرصده أي فحص**. صار
  السلوك واحدًا: الاسم المجهول خطأ.
- **🟠 اسم مستعار يطابق معرّف عنصر قائم كان يحجبه بصمت**، فسهم يقصد العنصر الموجود
  يرتبط بالجديد. الغموض يُرفض بـ`ALIAS_SHADOWS_ELEMENT` بدل أن يُحلّ بالحظ. ومعه:
  عنصر ليس كائنًا في الدفعة كان يُنتج `TypeError` خامًا لا خطأ جسر.
- **🟡 هندسة غير عددية تُنتج خطأ تداخل وهميًا** («تقاطع جزئي NaN×NaN») يُسقط النتيجة
  ويُعلّم الوكيل تجاهل الأداة — وهو نفس ما أصلحناه في عدّ الاحتواء. `import_scene`
  لا يتحقّق من العناصر فالمسار مفتوح. صارت تُستبعد من فحص التداخل ويُبلَّغ عنها
  `invalid_geometry`.

وثبت سليمًا تحت كل مدخل عدائي: `randomId` (٨ محارف، الأبجدية الصحيحة، ٢٠٠٠٠٠
معرّف بلا تكرار)، وتسلسل `enqueue` بما فيه بقاء الطابور سليمًا بعد رفض متزامن أو
غير متزامن، وترتيب `ids[]` بترتيب المتصل، ومسار الارتباط في `updateElement` كاملًا
بما فيه تنظيف `boundElements` من الطرف القديم، وتصنيف الاحتواء واستثناؤه من
النتيجة، وعناوين الموصلات المستثناة من فحص فيض النص.

### تكافؤ الأدوات بين الخادم والجسر

فحص جديد يمسح الجهتين: **أداة معلَنة بلا منفّذ تنجح في `tools/list` وتفشل عند أول
نداء**، والعدّ وحده لا يكشفها. يتحقّق الفحص من أن لكل أداة من الـ59 منفّذًا (في
الجسر أو في الخادم محليًا)، وأن كل طريقة RPC يناديها الخادم موزَّعة في الجسر، وأن
كل معالج مُنادى في `dispatch` معرَّف فعلًا (62 معالجًا)، وأن لا أسماء مكرَّرة، وأن
لكل أداة مخطط دخل ووصف. النتيجة الحالية سليمة تمامًا — والفحص يمنع الانحدار.

### إصلاحات صغيرة تابعة

- **مجلد كان يُقبل كخط** في الجسر والطبيب: `getAbstractFileByPath` يعيد `TFolder`
  و`fs.access` ينجح على المجلدات. صار الشرط `TFile` و`stat().isFile()`.
- **مسار خارج الخزنة** (مطلق، أو صاعد بـ`..`، أو بشرطة خلفية) كان يُبلَّغ عنه
  «جاهزًا» وExcalidraw لا يحمّله. صار يُعدّ غير مسجَّل — بالشرط نفسه في الموضعين
  حتى لا يختلف تقرير الجسر عن تقرير الطبيب.
- **إعدادات معطوبة** كانت تُقرأ «لا خط مثبَّت» — كذبٌ يُنقل إلى المستخدم عن خزنة
  فيها خط سليم. صارت تُبلَّغ `unreadable: true` مع «الحالة مجهولة».
- الطبيب يستخدم ثابت المعرّف بدل نصّ مكرّر، وعلامة سطر الخط من طاقم العلامات
  نفسه (`✓` / `–`).

### وثائق كانت تُبطل القاعدة

الطلب الجاهز في `START-HERE-AR.md` — وهو **النص الذي يُلصق فعلًا في الوكيل** —
كان يقول «استخدم خط العائلة الرابعة»، و`PROFESSIONAL-GUIDE-AR.md` يقول إن العائلة
الرابعة هي خط الخزنة بلا شرط. قاعدة في `AGENTS.md` لا تنفع أمام أمرٍ صريح في
النص المُلصق. صار الاثنان مشروطين بـ`status`، ويحرسهما اختبار يمسح كل وثيقة
مُوزَّعة ويرفض أي ذكر غير مشروط للعائلة الرابعة.

وصُحِّح ادعاءان: الجدول كان يقول إن `.mcp.json` «يُلتزم في Git» وهو مُدرَج في
`.gitignore`؛ وكان يُطلب البحث عن سطر يبدأ بـ`Arabic font` وهو نصّ حالة واحدة من
أربع. وأُعيد علاج الحالة «الملف موجود والخيار مطفأ» الذي فُقد من الجدول.

### بوابة النشر: قيست بوكيل جديد لا بالرأي

سُلّمت نسخة نظيفة إلى وكيل لا يعرف المشروع، مع طلب المستخدم الحرفي وحده، ومُنع من
قراءة السجل أو الباتشات — ثم رُصدت كل نقطة يتوقّف فيها. النتيجة: **13 نقطة توقّف
وأربعة أسئلة حاجزة، وثلاث مرات اضطرّ لفتح ملف مصدر.** والمعيار المكتوب في التقرير
هو «أن يصل غريب إلى جاهز دون أن يسأل سؤالًا واحدًا».

**١. 🔴 الجملة الآمرة الوحيدة في وثيقة الدخول تفشل على Linux.**
`node install.mjs --clients all` كان يرفع استثناءً **قبل تثبيت أي شيء** على Linux
غير WSL، فلا يُضبط `project` ولا `codex` أيضًا مع أن ضبطهما ممكن تمامًا. الحرس كان
يمنع الخطأ الصحيح (مسارات Linux في إعداد Windows) بثمن إلغاء العملية كلها.
**الآن يُستثنى `claude-desktop` وحده**، ويُضبط الباقي، ويظهر `skippedClients` مع
خطوة يدوية — والفشل يبقى فشلًا فقط إن لم يبقَ عميل واحد يمكن ضبطه.

**٢. 🔴 `AGENTS.md` كان يذكر تفعيل الموصل كخطوة مطلقة** — وهو **أول ملف يقرأه
الوكيل**. أُصلح `START-HERE` و`TROUBLESHOOTING` في هذه المراجعة ونُسي هو، فمستخدم
Codex يُرسَل إلى زر لا وجود له ثم يظن نظامه معطوبًا. صار جدولًا بحسب العميل، ومعه
أمر صريح: **اسأل المستخدم عن عميله قبل أي شيء** — لم يكن في الملف ما يأمر بذلك
إطلاقًا، وجدول «اختر عميلك» موجّه للإنسان في `START-HERE` لا للوكيل.

**٣. 🔴 معيار النجاح لم يكن قابلًا للمطابقة.** الوثيقة تأمر بـ`--lang en` ثم تعطي
المعيار بعبارة عربية **لا تُطبع أبدًا** في ذلك المسار، و`README` يعطي معيارًا ثالثًا
مختلفًا. فأُضيف سطر أخير **ثابت لا يتغيّر بتغيّر اللغة**:

```
RESULT install=ready bridge=ok ready=true
```

وتحيل إليه الوثائق الثلاث. و`install.mjs` صار يعطي **`ok`** بالمعنى نفسه: `true`
تعني لم يبقَ عمل على المستخدم، و`ok:false` مع `manualSteps` تعني «ثُبّت وبقيت خطوة»
— و`installed: true` وحده كان يُطبع دائمًا فلا يفرّق الحالتين، ومن هنا جاء
«installed: true» الكاذب على نسخة المتجر. ورمز الخروج يتبع المعيار (`3`).

**٤. 🟠 `--node` مُلزَم بلا قيمة موثّقة.** `AGENTS.md` يُلزم بتمريره، ولا وثيقة تقول
ما قيمته ولا كيف تُعرَف — والوسيط الخاطئ يُهمَل **بصمت**. صار جدولًا بحسب مكان
العميل، مع `where.exe node` كطريقة معرفة، وتحذير صريح من `which node` في بيئة غير
بيئة العميل — وهو عين العطل الذي تمنعه القاعدة.

**٥. 🟠 البحث عن الخزنة بلا جذر ولا معيار.** الوكيل ثبّت على `/tmp/fonttest` — بقايا
اختبار. صار له جذر وعمق واستثناءات (`node_modules`، `.git`، `/tmp`، `test`،
`fixture`) وحالة **صفر خزنات** (اسأل) وحالة واحدة (أكّدها بذكر مسارها). وفحص وجود
Obsidian صار بأثره على القرص لا بالتطبيق، مع منع صريح لتنزيله في حاوية بلا واجهة.

**٦. 🟠 Cursor موعود في وثيقتين ولا يعرفه المثبّت.** و`--clients cursor` كان يمرّ بلا
خطأ فلا يضبط شيئًا ويخرج كأنه نجح. صار الاسم المجهول يُرفض بخطأ صريح يسمّي المدعوم
ويشرح مسار Cursor (`--clients project` ثم نسخ `.mcp.json`).

**٧. 🟠 `AGENTS.md` كان يخلط جمهورين.** أربع قراءات إلزامية في `.context/*` قبل «أي
عمل»، وأمرٌ بتحديث `STATE.md` — في مستودع المستخدم، لمهمة تثبيت. صار الملف يفصل
**مُعِدًّا** من **مساهم** في أول سطر، وبروتوكول المساهم انتقل إلى آخر الملف مع نصّ
صريح: لا تقرأ `.context/*` ولا تكتب فيها إن كنت تُعِدّ.

**٨. 🟡 `EXCALIDRAW_RPC_TIMEOUT_MS` بلا موضع ولا افتراضي.** صار موضعه موثّقًا لكل
عميل (`env` في `.mcp.json`، و`[mcp_servers.excalidraw.env]` في Codex) مع الافتراضي
`45000` وتحذير من رفعه فوق مهلة العميل.

### القاعدة: الوكيل يقرأ حقلًا، لا يفسّر نثرًا

قياسٌ ثانٍ بوكيل جديد — بعد إصلاحات البوابة — نزل إلى **أربعة أسئلة كلها مصرَّح بها
في الوثيقة**، ومرة واحدة فتح المصدر. وكان ما فتحه لأجله هو الدرس: **قيم `bridge=`
غير موثّقة في أي ملف**، ووثيقتان تنسبان كل حالة غير `ok` إلى «Obsidian مغلق» — وهي
**خاطئة في أربع من خمس**. وأشيعها `no-token` ومعناها «لم يُفتح رسم Excalidraw بعد»؛
فالوكيل يخبر المستخدم بإصلاح شيء سليم ويترك السبب الحقيقي.

فالحل ليس تحسين الجملة بل إلغاء الحاجة إليها:

- **كل حالة تحمل نفسها في `--json`:** `bridge.meaning` و`bridge.meaningEn` و
  `bridge.nextAction` و`bridge.nextActionEn` و`bridge.blame` (`user-action` /
  `environment` / `bug` / `unknown` / `none`). والمخرَج النصي يطبع المعنى والخطوة
  سطرين بعد النتيجة. فلا يفسّر أحدٌ حالةً بنفسه.
- **`ready:true` كان ممكنًا مع صفر تسجيل عميل** — أي طبيبٌ يقول «جاهز» ومستخدمٌ لا
  يرى أدوات. صار الطبيب يفحص `.mcp.json` و`~/.codex/config.toml` وإعدادات Claude
  Desktop على ويندوز، ويُبلَّغ في `clientRegistrations` وفي سطر النتيجة
  (`client=registered|unknown`). **ولا يُحتسب في `installReady`** لأن عميلًا قد
  يقرأ من مسار مخصّص — لكنه أشيع سبب لغياب الأدوات مع تثبيت سليم.
- **مسار خزنة غير موجود** كان يعطي مخرجًا **مطابقًا حرفًا بحرف** لخزنة حقيقية غير
  مثبَّتة، فخطأ مطبعي واحد يعني «التثبيت ناقص» إلى الأبد — والمسارات هنا عربية وفيها
  مسافات. صار يُميَّز: `vaultExists` و`vaultMissing`، و«موجود بلا `.obsidian`» حالة
  ثالثة لها رسالتها.
- **`--bridge-host` كان غائبًا من كل وثيقة** — وهو الوسيط الوحيد الذي قد يجعل الفحص
  الحيّ يعمل من حاوية أو WSL، أي **المحاولة قبل الاستسلام** بـ`--skip-live`. صار
  موثّقًا بأمثلة عناوين، ويحرسه اختبار يقارن وسائط `Usage` بما تذكره الوثائق.
- **`docs/INSTALLATION-AR.md` كان معزولًا** عن سلسلة الدخول: لا رابط إليه من
  `README` ولا `START-HERE` ولا `AGENTS`، وهو الملف الوحيد الذي يوثّق `--offline` و
  `--skip-official` و`--force-plugin-versions`. صار مرتبطًا، ويحرسه اختبار.
- **سياسة تثبيت Obsidian كانت ثلاث روايات** متعارضة. صارت واحدة: المستودع لا يثبّت
  التطبيق، وفي بيئة بلا واجهة لا يستطيع الوكيل ذلك فيطلب من المستخدم.
- وقاعدة التعرّف على سطر الخط كانت «السطر الوحيد المعلَّم بـ`–`» — وتنكسر عند
  التثبيت المكتمل لأن كل الأسطر تصير `✓`. صار الوكيل يُحال إلى `doctor --json`
  وحقل `font`، وهو يعمل **بلا Obsidian مفتوح** بخلاف `status.fonts`.

### حرس معمَّم بدل حرس ضيّق

البند ٢ عطلٌ **كان حرسي يجب أن يمسكه**: كتبت اختبارًا يرفض أي ذكر غير مشروط
للعائلة الرابعة، فمرّت خطوة الموصل مطلقةً لأن الحرس كان يمسح قاعدة واحدة لا
الصنف. صار الحرس يمسح **كل قاعدة تخصّ عميلًا** — الموصل، والعائلة الرابعة،
والإغلاق الكامل — في أربع وثائق موجّهة للوكيل، ويقرأ التقييد من العنوان أو من
ترويسة الجدول إن كان السطر صفًّا فيه، ويجرّد التشكيل قبل المطابقة فلا يسقط على
فرق حركة. وأُضيف فحص أن سطر `RESULT` واحد في اللغتين وأن الوثائق تحيل إليه.

### الاختبارات

54 اختبارًا (كانت 37)، و59 أداة. والأهم أن الاختبارات الجديدة **سلوكية لا نصية**:
تُستخرج `applyStyle` و`getFontStatus` وتُنفَّذان فعلًا مقابل بيئة Obsidian مُقلَّدة،
لأن الفحص النصي يمرّ على تحويلات تكسر السلوك. تحقّقنا بأربع عشرة طفرة — إعادة فرض
`1`، وحذف استعادة المقاس، وإلغاء استعادة العائلة، وربط الجاهزية بالمسار بدل
التفعيل، وقبول المجلدات، وإسقاط شرط الاحتواء، وتجاهل مفتاح التفعيل في الطبيب،
وربط رمز الخروج بالخط، وقبول الاسم المجرّد كمسار، وتخطّي طبقة الواجهة، وتخطّي طبقة
الإعدادات، وإلغاء الاستعادة كلها، وقبول المجلدات في الطبيب — **وكل واحدة تُسقط
اختبارًا** بينما تمرّ كلها على الصيغة النصية السابقة.

---

## 0.6.0 — إصلاحات جاهزية النشر

اكتُشفت هذه الأعطال في **استخدام حيّ متصل** على ويندوز مع نسخة MSIX من المتجر،
ولم يرصد أيًّا منها الفحص الساكن ولا اختبارات القبول القائمة.

### أعطال حرجة

- **تلوّث المشاهد بين الرسومات.** `open_drawing` لم يكن ينظّف ورشة
  `ExcalidrawAutomate` — وهي حالة عامة مشتركة — فتسرّبت عناصر رسمة إلى رسمة
  أخرى عند التنقّل بينهما. `createDrawing` كان ينظّفها بتعليق يحذّر من هذا
  بالضبط، و`openDrawing` لا. **يُنظَّف الآن قبل الفتح وبعده، ويُعاد ربط EA
  بالعرض الجديد صراحةً.**
- **`update_element` كان يقبل `startElementId`/`endElementId` ويعيد نجاحًا بلا
  ارتباط**، ويكتب الحقلين كخصائص خام غير قياسية. **صار يُنشئ
  `startBinding`/`endBinding` بالشكل الرسمي، ويُدرج السهم في `boundElements`
  للطرفين، ويُعيد التوجيه من نص داخل حاوية إلى الحاوية، ويتحقّق بعد التعديل —
  فيفشل بـ`BINDING_NOT_APPLIED` بدل النجاح الكاذب.**
- **`update_element` على نص كان يُرجع نجاحًا ولا يتغيّر شيء.** يتحقّق الآن
  ويفشل بـ`UPDATE_NOT_APPLIED` مع القيمة الفعلية.
- **معرّفات العميل أصبحت أسماء مستعارة.** لا يصل `id` الحر إلى قسم
  `Text Elements` بعد الآن؛ يولّد الجسر معرّفات أصلية ويحل مراجع الأسهم داخل
  الدفعة نفسها، ثم يعيد `resolvedId` و`idMappings` للمتابعة.
- **`move_note` قد يجمّد الجسر بالكامل.** `fileManager.renameFile` يشغّل تحديث
  روابط الخزنة كلها؛ في خزنة كبيرة يجمّد واجهة Obsidian فتتوقف كل عمليات الجسر
  خلفه في الطابور. أُضيف **`updateLinks: false`** لمسار سريع عبر `vault.rename`.

### تسرّب الأسلوب

- `ea.style` حالة عامة تبقى بين النداءات، فعنصر متقطع واحد كان يجعل **كل ما
  بعده** متقطعًا بشفافية موروثة. **تُستعاد الافتراضيات القياسية قبل كل تطبيق.**

### جودة الفحص

- `inspect_visual_quality` كان يعدّ **الاحتواء** تداخلًا، فيُصفّر نتيجة أي مخطط
  مسارات أو مناطق سليم — ويتعلّم الوكيل تجاهل الأداة. صار يميّز الاحتواء
  (`info`) من التقاطع الجزئي (`error`)، ولا تُحسب `info` في النتيجة.
- تحذير `ARROW_WITHOUT_BINDING` عند إنشاء سهم بلا `startElementId`.

### التثبيت

- **`install.mjs` كان يكتب في ملف لا يقرأه التطبيق.** نسخة MSIX تعزل إعدادها في
  `%LOCALAPPDATA%\Packages\Claude_*\LocalCache\...`، فالكتابة في
  `%APPDATA%\Claude` لا تصل — ومع ذلك كان يطبع `"installed": true`.
  `discoverWindowsClaudeConfigs()` في `platform-paths.mjs` تكتشف المسارين وتكتب
  في الموجود فعلًا، وتنشئ المسار المناسب في التثبيت الجديد.
- **`--node <path>`** لتحديد Node الذي سيستعمله **العميل** لا الذي يشغّل المثبّت.
- **تقرير صادق**: `manualStepRequired: true` إذا تعذر تحديد المسار، بدل «تم».
- تحقّق من Node 18 قبل أي شيء، ورسائل خطأ نظيفة بلا آثار تتبّع.

### الفحص

- **`doctor.mjs` v2**: فصل `installReady` عن `liveReady` — رمز الخروج يعبّر عن
  التثبيت، فلا يظهر «غير جاهز» لأن Obsidian مغلق. ومقارنة الإصدارات بملف القفل،
  و`--lang en` (العربية تتشوّش في PowerShell)، و`--skip-live`، و`--require-live`
  للـCI، ورسالة صادقة لرفض الاتصال تغطي «Obsidian مغلق» و«مضيف مختلف».
- **`validate-scene.mjs` جديد**: فحص سلامة الرسومات **بلا Obsidian** — أسهم غير
  مرتبطة، حقول غير قياسية، خصائص ناقصة، معرّفات قصيرة، مراجع معلّقة. وهو الفحص
  الذي كان غائبًا فمرّ عيب الأسهم إلى النشر. مضاف إلى `npm run check` مع
  `tests/fixtures/` واختبارَي انحدار.
- **`lib/lz-string.mjs` جديد**: ضغط وفك ضغط `compressed-json` بلا أي اعتمادية،
  ومخرجاته مطابقة بايت-ببايت لمكتبة المرجع.
- `server.mjs`: مهلة الطلب **45s** بدل 120s. كانت أطول من مهلة عملاء MCP (60s)،
  فيرى الوكيل مهلة عميله بدل خطأ الجسر — ويحسب عمليةً ناجحةً فاشلةً فيعيدها.
  قابلة للضبط بـ`EXCALIDRAW_RPC_TIMEOUT_MS`.

### الوثائق (وهي حاجز النشر الحقيقي)

- **خطوة تفعيل الموصل** أُضيفت إلى `START-HERE-AR.md` و`TROUBLESHOOTING-AR.md`.
  كانت **غائبة تمامًا** من المستودع (صفر ذكر في كل الملفات) وهي توقف كل مستخدم:
  الخادم المسجَّل لا تصل أدواته حتى يُفعَّل بالزر، ولا شيء في الشاشة يدلّ عليها.
- قسم MSIX في استكشاف الأخطاء، وتحذير PowerShell المرفوع الصلاحيات.
- `docs/TROUBLESHOOTING-AR.md` مرتّب بحسب العَرَض، وأوّله `--self-test` الذي يفصل
  «الخادم سليم والتسجيل هو المشكلة» عن «الخادم لا يعمل».
- `AGENTS.md`: فصل «الوكيل في بيئة معزولة»، و**خط أحمر صريح**: لا يُكتب في
  `.excalidraw.md` من خارج الإضافة — النص مخزَّن في مكانين، وتحديث أحدهما دون
  الآخر يُفرّغ النصوص بصمت.

### معروف ولم يُصلح

- الرسومات التي أنشأتها إصدارات سابقة أسهمها غير مرتبطة. رمّمها بأدوات MCP بعد
  هذا الإصدار (`update_element` صار يعمل)، لا بالكتابة على الملف.
- المعرّفات الأقصر من 8 محارف في رسومات قديمة تُترك كما هي؛ تغييرها يكسر المراجع.
