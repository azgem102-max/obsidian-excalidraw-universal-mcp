# الخطوط المحلية

لا يحتوي الريبو ملفات خطوط. ضع هنا ملف خط تملك حق استخدامه ثم مرره للمثبت:

```bash
node install.mjs --vault "<VAULT>" --clients all --font "assets/fonts/my-font.woff2"
```

المثبت ينسخه إلى `Excalidraw/Custom Fonts/` داخل الـVault ويفعّل خيار الخط الرابع.

