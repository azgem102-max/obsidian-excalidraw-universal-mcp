# Excalidraw Universal MCP — دليل وكلاء الذكاء الاصطناعي

يقرأ Codex هذا الملف مباشرة، ويقرأه Claude عبر `CLAUDE.md`.

## هذا الملف جمهوران — اعرف أيّهما أنت

- **مُعِدّ:** طُلب منك تجهيز جهاز. اقرأ «الإعداد لأول مرة» أدناه **ولا شيء غيره**.
  لا تقرأ `.context/*` ولا تكتب فيها: أنت في مستودع المستخدم لا في مستودع تطوير،
  والإعداد ليس «عملًا ذا معنى» يوجب تحديث سجل الحالة.
- **مساهم:** تعدّل كود المستودع. اتبع «بروتوكول المساهم» في آخر الملف.

## إذا طلب المستخدم الإعداد لأول مرة

### 0) اسأل سؤالين قبل أي شيء — إن لم تكن تعرف الجواب

1. **أي عميل MCP تستخدم؟** `Codex` أم `Claude Code` أم `Claude Desktop`. لا تخمّن:
   خطوات ما بعد التثبيت تختلف، وخطوة تفعيل الموصل **إلزامية في Claude Desktop
   وحده**. وإن أراد الثلاثة فـ`--clients all`.
2. **وعلى أي جهاز؟** إن كنت تعمل في Linux أو حاوية والعميل على Windows، فأنت في
   حالة «مضيفَين» — انظر البند 5.

وسؤال ثالث **فقط إن لم تجد خزنة**: مسار خزنة Obsidian.

### 1) اعرف بيئتك ثم ابحث عن الخزنة

- `node --version` ≥ 18.
- **وجود Obsidian:** لا تفحص التطبيق نفسه — افحص أثره. مجلد `.obsidian` بداخله
  `plugins/` أو `community-plugins.json` يعني خزنة حقيقية استُخدمت مرة. وإن لم
  يوجد أي مجلد كهذا فقد لا يكون Obsidian مثبتًا أو لم يُفتح بعد: **اسأل ولا
  تثبّت**. ولا تنزّل التطبيق داخل حاوية بلا واجهة؛ اطلب من المستخدم فتحه مرة.
- **البحث:** ابدأ من مجلد المستخدم بعمق ثلاثة مستويات، وابحث في
  `~/Documents` و`~/Obsidian` و`~/Desktop` أولًا. **استبعد** `node_modules`
  و`.git` و`/tmp` وأي مسار فيه `test` أو `fixture` أو `sample`.
- **صفر خزنات** ⇒ اسأل. **أكثر من واحدة** ⇒ اسأل أيّها. **واحدة** ⇒ أكّدها بذكر
  مسارها قبل التثبيت، لا تفترضها.

### 2) ثبّت

- **على Windows ومعك واجهة:** `setup-windows.cmd` بالنقر المزدوج، ويختار الخزنة
  من نافذة. لا تطلب منه كتابة أمر إذا كانت الواجهة متاحة.
- **غير ذلك:**

```
node install.mjs --vault "<VAULT>" --clients <CLIENTS> --project-root "." --node "<NODE>"
```

`<CLIENTS>` من جواب السؤال الأول. و`--font "<FONT>"` إن أعطاك خطًا؛ ولا تنزّل خطًا
من مصدر غير رسمي ولا ترفعه إلى Git.

### 3) `--node` — القيمة الصحيحة، لا `which node`

`--node` هو **المسار الذي سيشغّله العميل**، لا الذي تشغّل به أنت. وإن كنت في بيئة
غير بيئة العميل فتمريره **إلزامي**، وإلا كُتب مسارك في إعداده فلا يعمل أبدًا.

| العميل يعمل على | القيمة |
|---|---|
| Windows | `C:\Program Files\nodejs\node.exe` — تحقّق بـ`where.exe node` من PowerShell |
| macOS / Linux، ونفس بيئتك | اتركه؛ الافتراضي صحيح |
| بيئة غير بيئتك | اسأل المستخدم عن ناتج `where.exe node` أو `which node` عنده |

**لا تخمّن.** الوسيط المجهول أو المسار الخاطئ لا يُنبّهك.

### 4) اطلب من المستخدم — بحسب عميله

للجميع: إعادة تشغيل Obsidian **وفتح أي رسم Excalidraw مرة واحدة** (هي التي تولّد
رمز الجسر وتشغّل المنفذ)، ثم إعادة تشغيل العميل.

| العميل | إعادة التشغيل | تفعيل الموصل |
|---|---|---|
| Codex | الجلسة | **لا يحتاج** |
| Claude Code | الجلسة | **لا يحتاج** |
| Claude Desktop | **إغلاق كامل** من أيقونة شريط المهام، لا بـ✕ | **نعم — إلزامي بالزر** |

سطر تفعيل الموصل هو أكثر ما يتوقف عنده الناس على Claude Desktop، ولا شيء في
الشاشة يدلّ عليه. **ولا تذكره لمستخدم Codex أو Claude Code إطلاقًا** — سيبحث عن زر
لا وجود له ثم يظن نظامه معطوبًا.

### 5) حالة المضيفَين: أنت في مكان والعميل في مكان آخر

- **WSL والعميل على Windows:** أبقِ الريبو والخزنة على قرص Windows مثل `/mnt/c`
  وشغّل المثبّت نفسه؛ فهو يحوّل المسارات. لا تكتب إعداد Claude Desktop يدويًا
  بمسارات Linux. وإن منعت الصلاحيات الكتابة في `%APPDATA%` فأعطِ المستخدم أمر
  PowerShell الذي يطبعه المثبّت.
- **Linux أو حاوية منفصلة عن Windows:** `claude-desktop` **يُستثنى تلقائيًا** ويُضبط
  الباقي، ويظهر في المخرجات `skippedClients` مع خطوة يدوية. أكمل `project`
  و`codex` ثم أعطِ المستخدم `setup-windows.cmd` لإكمال Claude Desktop على Windows.
- **Linux أصلي والعميل عليه:** `--clients project,codex` يعمل عاديًا.

### 6) معيار النجاح — اقرأه من حقل لا من عبارة

```
node doctor.mjs --vault "<VAULT>" --json
```

الحقول: **`installReady`** (التثبيت) و**`liveReady`** (الجسر حيّ) و**`ready`**
(الاثنان). وفي المخرجات النصية سطر أخير ثابت **لا يتغيّر بتغيّر اللغة**:

```
RESULT install=ready bridge=ok client=registered ready=true
```

طابق هذا، **لا** عبارات الواجهة — فهي تُترجَم، والوثائق توصي بـ`--lang en` على
PowerShell فلا تظهر العبارة العربية أبدًا.

ورمز الخروج يعبّر عن **التثبيت وحده**: `installReady:true` مع `liveReady:false`
يعني تثبيتًا سليمًا وObsidian مغلقًا. ولربط الخروج بالجسر أيضًا: `--require-live`.

و`install.mjs` يعطي **`ok`** بالمعنى نفسه: `true` تعني لم يبقَ عمل على المستخدم.
`ok:false` مع `manualSteps` تعني أن التثبيت جرى وبقيت خطوة لا يستطيعها المثبّت —
**لا تقل «جاهز» حينها**، انقل الخطوة كما هي.

ولا تشغّل اختبارات القبول الطويلة (`live-acceptance.mjs`) للمستخدم النهائي.

### 7) إن لم تصل إلى الجاهزية من بيئتك

الحاوية وVM وWSL لا تصل إلى `127.0.0.1` للمضيف. فشل الفحص الحيّ هنا **«غير قابل
للفحص من هنا» لا «فاشل»**.

**جرّب قبل أن تستسلم:** `--bridge-host <host>` يوجّه الفحص إلى عنوان يصل إلى المضيف
(من حاوية Docker غالبًا `172.17.0.1` أو `host.docker.internal`، ومن WSL2 عنوان
المضيف في `/etc/resolv.conf`). فإن نجح فلديك جاهزية حقيقية لا تخمين.

وإن لم ينجح استخدم `node doctor.mjs --skip-live` (وسيط الطبيب لا المثبّت)، وأبلغ
المستخدم صراحةً: «التثبيت مكتمل، والجاهزية الحية تحتاج تشغيل
الأمر على الجهاز نفسه» — ثم أعطِه الأمر جاهزًا. لا تقل «جاهز» ولا «فاشل».

## قواعد الاستخدام اليومي

- استخدم أدوات MCP بدل تعديل ملفات `.excalidraw.md` الخام.
- للملاحظات: اقرأ أولًا، ثم حدّث المحتوى أو الخصائص مع الحفاظ على الروابط.
- للرسومات: صف المشهد، خذ لقطة قبل التعديل الكبير، أنشئ العناصر دفعة واحدة، ثم التقط صورة وراجع التداخل والقص ومسارات الأسهم.
- **الخط: اقرأه من `status`، لا تفترض رقمًا.** `status` يعيد
  `fonts.arabicFontFamily`. إن كان `4` مرّره في `fontFamily` للنص العربي، وإن كان
  `null` **فلا تمرّر `fontFamily` إطلاقًا** واترك افتراض الخزنة. المستودع لا يوزّع
  خطًا — يضيفه المستخدم بنفسه بـ`--font` — ففرض `4` على خزنة بلا خط يضيّع النص،
  وفرض `1` يُلغي خط من ثبّته. والجسر لا يخمّن أيضًا: لا يلمس `fontFamily` أبدًا ما
  لم تطلبه صريحًا. والعائلات 1-3 لا تشكّل العربية.
- **مرّر `startElementId` و`endElementId` عند إنشاء أي سهم بين شكلين.** بدونهما
  السهم خطٌّ لا يتبع الأشكال، ولا يراه `Auto Layout` لأنه يقرأ
  `startBinding.elementId` ويُسقط بصمت ما لا ارتباط له.
- يمكنك إعطاء العناصر أسماء سهلة في `id` داخل الدفعة الواحدة؛ يعاملها الجسر
  كأسماء مستعارة، ويولّد معرّفات Excalidraw أصلية ثم يحل مراجع الأسهم تلقائيًا.
  استخدم `resolvedId` أو `idMappings` في النداءات التالية.
- **نادِ `open_drawing` قبل أي سلسلة تعديل** ولا تعتمد على الرسم النشط.
- بعد `Uniform size` شغّل `Fixed spacing`؛ الأول لا يعيد التوزيع فتتلامس الصناديق.
- `move_note` يشغّل تحديث روابط الخزنة كلها وقد يجمّد الواجهة؛ استخدم
  `updateLinks: false` إن لم تكن الروابط مهمة.
- لا تفترض أن قيمة `id` التي أرسلتها هي معرّف الملف؛ هي اسم مستعار فقط. استخدم
  `resolvedId` أو `idMappings` الذي يعيده الجسر في الطلبات التالية.
- اجعل العناوين 20px أو أكبر والنص 16px أو أكبر، واترك 40px بين العناصر.
- لا تضع عنوانًا مرتبطًا داخل مستطيل خلفية كبير؛ استخدم نصًا حرًا أعلى المنطقة.
- استخدم الأسهم المستقيمة للمسار المباشر، والمنحنية للتفرع، والمتعرجة/ذات الزوايا للالتفاف حول العوائق.
- استخدم الظل للإبراز فقط، ولا تكرر الزينة في كل عنصر.
- قبل المسح أو الاستبدال خذ snapshot. الحذف من الملاحظات يجب أن يكون إلى سلة Obsidian.
- لا تستدعِ `export_to_excalidraw_url` إلا بطلب صريح لأنه اتصال خارجي.

### إذا وصلك `OPERATION_TIMEOUT`

المهلة حدّ للاستجابة وليست إلغاءً للعمل داخل Obsidian. يبقى طابور الجسر محجوزًا
حتى تنتهي العملية الحقيقية، فلا تدخل عملية لاحقة على مشهد نصف مكتمل. وإذا حجبت
Mermaid خيط الواجهة فقد يكتمل التحويل بعد 30 ثانية ويعود ناجحًا؛ هذا متوقع.

نادِ `describe_scene` أولًا. في `create_from_mermaid` تحديدًا، إعادة **الطلب المطابق**
في الرسم نفسه خلال عشر دقائق آمنة: يعيد الجسر نتيجة العملية الأولى ولا ينشئ نسخة
ثانية. لا تمرر `forceDuplicate: true` إلا عندما تريد إضافة المخطط نفسه مرة أخرى قصدًا.

مهلة استجابة الجسر 30 ثانية عندما تسمح الواجهة للمؤقت بالعمل، ومهلة نقل الخادم
45 ثانية افتراضيًا، ثم مهلة العميل. لعمل أطول يمكن ضبط `EXCALIDRAW_RPC_TIMEOUT_MS`.

دورة العمل البصرية المعتمدة موضحة في [مخطط دورة عمل الوكيل](docs/دورة%20عمل%20الوكيل.png)،
وطبقات الأعطال موضحة في [مخطط أين تنكسر السلسلة](docs/أين%20تنكسر%20السلسلة.png).

## خط أحمر: لا تكتب في `.excalidraw.md` من خارج الإضافة

الملف يخزّن نص العناصر في **مكانين**: JSON المضغوط، وقسم `## Text Elements`
بمراجع `^blockId`. أي كاتب خارجي يحدّث أحدهما دون الآخر **يُفرّغ النصوص بصمت**
ويظن أنه نجح — وقد حدث ذلك فعلًا في أداة ترميم مكتوبة بعناية.

- للقراءة والفحص: `validate-scene.mjs` آمن تمامًا.
- للتعديل: **أدوات MCP فقط**.
- Obsidian يحفظ الرسم المفتوح دوريًا **فيكتب فوق أي تعديل خارجي، بل يدمج معه
  فيضاعف العناصر**. لا تلمس ملفًا مفتوحًا.

## إذا كنت تعمل في بيئة معزولة

حاوية سحابية أو WSL أو VM أو وكيل CI — والتفصيل في البندين 5 و7 أعلاه. وزيادةً:

1. **لا تطلب من المستخدم تحرير JSON بيده** — ولّد أمرًا يضيف المفتاح برمجيًا
   بنسخة احتياطية. خطأ الطبقة أو القوس الناقص يُسقط الإعداد بلا رسالة.
2. **تحقّق من الخزنة بالقراءة والمقارنة** قبل أي تثبيت — كثيرًا ما يكون مكتملًا.
3. **اجعل مخرجات أوامر PowerShell إنجليزية** (`--lang en`) — ومعيار النجاح هو سطر
   `RESULT` أو حقول `--json`، لا العبارة المترجَمة.
4. **لا تستخدم `$env:LOCALAPPDATA`/`$env:APPDATA`** في أوامر تسلّمها؛ مع الصلاحيات
   المرفوعة تشير إلى ملف تعريف Administrator فيفشل البحث بصمت.

### `EXCALIDRAW_RPC_TIMEOUT_MS` — أين يوضع

الخادم يشغّله **العميل** لا أنت، فالمتغيّر يوضع في `env` داخل إعداد العميل:

- `.mcp.json` أو إعداد Claude Desktop: `"env": { "EXCALIDRAW_RPC_TIMEOUT_MS": "90000" }`
- `~/.codex/config.toml`: تحت `[mcp_servers.excalidraw.env]`

القيمة بالمللي ثانية، والافتراضي `45000`. لا ترفعه فوق مهلة العميل نفسه. وحتى إن
انتهت مهلة النقل، تبقى عملية Obsidian محمية وإعادة Mermaid المطابقة لا تتكرر.

## خريطة الملفات

- `server.mjs` — خادم MCP الموحد، 59 أداة.
- `obsidian-plugin/` — الجسر المحلي داخل Obsidian.
- `install.mjs` — المثبت المتعدد العملاء.
- `doctor.mjs` — فحص جاهزية سريع وغير هدّام.
- `base-scripts/` — 17 سكربت تخطيط مع أيقوناتها.
- `professional-scripts/` — 15 سكربتًا احترافيًا.
- `docs/` — التثبيت والاستخدام والبنية والأعطال.
- `tests/` — اختبارات المطور، وليست مطلوبة للمستخدم النهائي.

## أوامر التطوير

- `npm run check` — فحص المصدر والاختبارات الساكنة و59 أداة وفحص المشاهد.
- `node validate-scene.mjs --path <vault>` — فحص سلامة الرسومات **بلا Obsidian**.
  هذا هو الفحص الذي يمسك أعطال المخرجات قبل النشر؛ بقية الاختبارات تحتاج واجهة.
- `node server.mjs --self-test` — فحص تعريف أدوات MCP.
- `node tests/live-acceptance.mjs ...` — قبول حي داخل Vault اختبار فقط.
- `node tests/professional-live-acceptance.mjs ...` — قبول السكربتات الاحترافية القابلة للأتمتة مع تسجيل البقية كتفاعلية.
- `node tests/tool-coverage-live-acceptance.mjs ...` — تغطية الأدوات المكملة؛ المشاركة الخارجية لا تعمل إلا مع `--include-external-share`.

---

## بروتوكول المساهم — لا يخصّ الإعداد

يخصّ من يعدّل كود هذا المستودع فقط:

1. قبل أي عمل اقرأ `.context/STATE.md`.
2. قبل تعديل الكود اقرأ `.context/CONVENTIONS.md`.
3. قبل تغيير معماري اقرأ `.context/DECISIONS.md`.
4. قبل تشخيص مشكلة اقرأ `.context/LESSONS.md`.
5. بعد عمل ذي معنى حدّث `STATE.md` باختصار وأبقِه أقل من 40 سطرًا.
6. لا تضع رموز MCP أو مسارات Vault شخصية أو خطوطًا غير مرخصة داخل Git.

## الهدف

حزمة محلية تجعل Claude Code وClaude Desktop وCodex والعملاء المتوافقين مع MCP
يقرأون ويكتبون ملاحظات Obsidian ويتعاملون مع رسومات Excalidraw الأصلية
والسكربتات من خادم واحد اسمه `excalidraw`. والمثبّت يضبط ثلاثة عملاء:
`project` و`codex` و`claude-desktop`. وعميل يقرأ إعداد MCP من مجلد المشروع (مثل
Cursor) يُخدَم بـ`--clients project` ثم نسخ `.mcp.json` إلى المكان الذي يقرأه —
والاسم غير المعروف يُرفض بخطأ صريح بدل أن يمرّ بلا أثر.
