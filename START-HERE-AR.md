# ابدأ من هنا — Obsidian وExcalidraw لأي وكيل AI

هذا هو الملف الوحيد الذي يحتاجه المستخدم في البداية. أعطِ الريبو إلى Claude Code أو Codex أو أي وكيل محلي قادر على تشغيل الأوامر وقل له:

> اقرأ `CLAUDE.md` أو `AGENTS.md`، ثم جهّز هذا الجهاز لاستخدام Obsidian وExcalidraw عبر MCP. ابحث عن الـVault إن أمكن، وإن وجدت أكثر من واحد فاسألني أيها أختار. نفّذ المثبت، اطلب مني إعادة تشغيل Obsidian والعميل مرة واحدة، ثم شغّل فحص الجاهزية فقط. لا تشغّل اختبارات التطوير الطويلة.

## ما الذي سيُثبت؟

1. **Obsidian** — التطبيق المضيف. إذا لم يكن مثبتًا يطلب الوكيل إذن المستخدم ثم يثبته من المصدر الرسمي أو يفتح صفحة التنزيل.
2. **Excalidraw 2.25.3** — الرسم الأصلي داخل Obsidian.
3. **Excalidraw Extras 0.0.15** — Mermaid وMathJax/LaTeX والطباعة والمكونات الثقيلة.
4. **Excalidraw Universal MCP Bridge 0.6.0** — القراءة والكتابة والرسم والسكربتات من Claude وCodex.
5. **32 سكربتًا** — 17 أداة تخطيط أساسية و15 سكربتًا احترافيًا.
6. إعداد MCP محلي باسم `excalidraw` للعميل المختار.

## اختر عميلك أولًا

الثلاثة مدعومون بالتساوي، **لكن خطوات ما بعد التثبيت تختلف** — وأكثر من يتوقف
يتوقف لأنه اتبع خطوات عميل آخر:

| | **Codex / ChatGPT** | **Claude Code** (طرفية) | **Claude Desktop** |
|---|---|---|---|
| اسم العميل في المثبّت | `--clients codex` | `--clients project` | `--clients claude-desktop` |
| ملف الإعداد | `~/.codex/config.toml`<br>`[mcp_servers.excalidraw]` | `.mcp.json` في جذر المشروع | JSON الخاص بالتطبيق — يكتشفه المثبّت |
| نطاق التسجيل | المستخدم — كل المجلدات | المشروع وحده | التطبيق كله |
| ملف التعليمات الذي يقرأه | `AGENTS.md` | `CLAUDE.md` (يحيل إليه) | لا يقرأ ملفًا — عرّفه بالمهارة أو المحادثة |
| **مفتاح تفعيل الموصل** | **لا يحتاج** | **لا يحتاج** | **⚠️ نعم — إلزامي** |
| بعد تغيير الإعداد | أعد تشغيل الجلسة | أعد تشغيل الجلسة | **أغلق التطبيق كاملًا** من أيقونة شريط المهام |

للجميع: `--clients all` يضبط الثلاثة معًا.

**الفرق الحاسم في السطر قبل الأخير.** في Codex وClaude Code يعمل الخادم بمجرد
تسجيله. أما Claude Desktop **فلا تصل أدواته حتى تفعّل موصل `excalidraw` بالزر**،
مهما كان الإعداد صحيحًا — ولا شيء في الشاشة يدلّ على ذلك. إن كنت على Codex فتجاوز
الخطوة 3 أدناه؛ وإن كنت على Claude Desktop فهي أهم خطوة في الصفحة.

## الطريقة الأسهل على Windows

بعد فك ضغط المشروع، اضغط مرتين على:

`setup-windows.cmd`

ستظهر نافذة لاختيار خزنة Obsidian، ثم يُثبّت كل شيء ويضبط Claude وCodex تلقائيًا. لا يحتاج المستخدم إلى معرفة المسار أو تعديل أي ملف. وإذا كان Node.js مفقودًا، يطلب الملف الموافقة قبل تثبيت النسخة الرسمية المستقرة.

## أمر التثبيت الموحد

يتطلب Node.js 18 أو أحدث وObsidian Vault موجودًا مرة واحدة على الأقل:

```powershell
node install.mjs --vault "C:\Users\name\Documents\Obsidian Vault" --clients all --project-root "."
```

إذا كان الوكيل يعمل داخل Linux منفصل عن Windows، يشغّل المستخدم من Windows PowerShell داخل مجلد الريبو:

```powershell
.\install-on-windows.ps1 -Clients "claude-desktop"
```

على macOS أو Linux يتغير مسار الـVault فقط:

```bash
node install.mjs --vault "$HOME/Documents/Obsidian Vault" --clients all --project-root "."
```

لإضافة خط عربي محلي يملكه المستخدم:

```powershell
node install.mjs --vault "C:\path\to\Vault" --clients all --font "C:\path\to\arabic-font.woff2"
```

لا يوزع الريبو خط «ثمانية» لأن صفحة التنزيل الرسمية تتطلب الحصول عليه من صاحبه مباشرة. يدعم المثبت أي ملف `otf` أو `ttf` أو `woff` أو `woff2` يملكه المستخدم، ويجعله الخط الرابع داخل Excalidraw تلقائيًا.

## بعد التثبيت

### 1) أعد تشغيل Obsidian وافتح أي رسم مرة واحدة

ليست خطوة شكلية: فتح رسم Excalidraw مرة واحدة هو ما **يولّد رمز الجسر ويشغّل
المنفذ المحلي**. بدونها لن يتصل المساعد بخزنتك.

### 2) أعد تشغيل العميل

- **Codex أو Claude Code:** أعد تشغيل الجلسة فقط. ثم انتقل إلى الخطوة 4.
- **Claude Desktop:** أغلقه من **أيقونة شريط المهام**، لا بالضغط على ✕ — وإلا بقي
  يعمل في الخلفية بالإعداد القديم.

### 3) ⚠️ فعّل موصل `excalidraw` بالزر — **Claude Desktop فقط**

قائمة الموصلات ← فعّل `excalidraw`.

**الموصل المسجَّل لا تصل أدواته حتى تفعّله**، مهما كان الإعداد صحيحًا. هذه أكثر
نقطة يتوقف عندها الناس، ولا شيء في الشاشة يدلّ عليها.

Codex وClaude Code لا يحتاجان هذه الخطوة إطلاقًا.

### 4) شغّل فحص الجاهزية

```powershell
node doctor.mjs --vault "C:\path\to\Vault"
```

على PowerShell أضف `--lang en`؛ صدَفة ويندوز تعرض العربية مشوّشة.

المتوقّع: `التثبيت مكتمل` ثم `الجسر حيّ — النظام جاهز للاستخدام`. والقسمان
منفصلان بقصد: «التثبيت مكتمل» مع فشل الاتصال الحيّ يعني أن التثبيت سليم وأن
Obsidian مغلق فقط.

ويطبع الطبيب سطرًا عن **الخط العربي**. المستودع لا يوزّع خطًا؛ إن لم تضف خطك فلن
تُشكَّل العربية في الرسومات، والسطر يخبر الوكيل بذلك صريحًا فلا يخمّن. لإضافته:
`node install.mjs --vault "<path>" --font "<font>"`.

### إن لم تظهر الأدوات

اقرأ `docs/TROUBLESHOOTING-AR.md` — أوّله الحالتان الأكثر تكرارًا: **الموصل
مطفأ**، و**الإعداد في ملف لا يقرأه التطبيق** (نسخة متجر مايكروسوفت تعزل إعدادها
داخل `AppData\Local\Packages\Claude_...`).

## أول طلب عملي

> افتح ملاحظة جديدة باسم «تجربة النظام»، اكتب فيها ملخصًا عربيًا، ثم أنشئ رسم Excalidraw منظمًا يوضح الفكرة. اقرأ الخط من `status` قبل الكتابة — إن لم يكن هناك خط عربي فلا تمرّر `fontFamily` إطلاقًا. ثم التقط صورة للرسم، أصلح أي تداخل، واحفظه.

## صورتان تختصران النظام

- [أين تنكسر السلسلة](docs/أين%20تنكسر%20السلسلة.png) — يوضح أين تقع مشكلات التسجيل والعميل والجسر والملف.
- [دورة عمل الوكيل](docs/دورة%20عمل%20الوكيل.png) — تحقق، أنشئ، نظّم بالسكربتات، راجع الصورة، ثم احفظ.

لا يحتاج المستخدم النهائي إلى تشغيل `live-acceptance.mjs`. هذا اختبار تطوير وصيانة، أما `doctor.mjs` فهو فحص الجاهزية اليومي.
